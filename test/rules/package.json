{
  "name": "house-manager-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security rules tests. Run with: npm test",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project house-manager-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
